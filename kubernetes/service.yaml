apiVersion: v1
kind: Service
metadata:
  name: churn-service
  labels:
    app: churn-mlops
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: churn-mlops